'use client'

type Props = {
  className?: string
}

/**
 * Bottom-right zoom slider panel — intentionally hidden on all flow canvases.
 * Zoom remains available via React Flow Controls, minimap, and scroll wheel.
 */
export function FlowZoomSlider(_props: Props) {
  return null
}
