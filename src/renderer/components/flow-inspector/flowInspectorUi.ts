/** Shared inspector section title styling (Node, Group, Note, Edge). */
export const FLOW_INSPECTOR_SECTION_LABEL =
  'text-xs font-semibold uppercase tracking-wide text-muted-foreground'

/** Per-field reset link — one step larger than legacy 10px. */
export const FLOW_INSPECTOR_RESET_LINK =
  'shrink-0 text-[11px] leading-none text-muted-foreground underline-offset-2 hover:text-foreground hover:underline disabled:pointer-events-none disabled:opacity-40'

/** Preview row reset-all ghost button. */
export const FLOW_INSPECTOR_RESET_ALL_BUTTON =
  'h-6 shrink-0 px-2 py-0 text-[11px] font-medium text-muted-foreground hover:text-foreground'
